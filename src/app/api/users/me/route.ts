// grabbing the information form the token data about the user 

import { getDataFromToken } from "@/helpers/getDataFromToken";

import { NextResponse, NextRequest } from "next/server";
import User from "@/models/userModel"
